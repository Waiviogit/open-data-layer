/** Zoom for embedded object geo preview (legacy sidebar map ~ neighbourhood context). */
export const OBJECT_MAP_PREVIEW_ZOOM = 14;

/** Minimum height so Leaflet MapContainer lays out tiles reliably. */
export const OBJECT_MAP_PREVIEW_MIN_HEIGHT_PX = 208;
