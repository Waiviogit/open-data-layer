import { ProfileRouteStub } from '@/modules/user-profile';

export default function UserProfileFeedHomePage() {
  return (
    <ProfileRouteStub
      title="Feed"
      description="Posts (default), threads, comments, mentions, activity."
    />
  );
}
