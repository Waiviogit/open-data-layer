import { ProfileRouteStub } from '@/components/user-profile/profile-route-stub';

export default function UserProfileWaivTablePage() {
  return (
    <ProfileRouteStub
      title="WAIV table"
      description="Standard / generated tabs via ?tab= on this route; shell sidebars hidden when table mode is open."
    />
  );
}
