{
  "name": "migrator",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "docker-image": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo CI builds the migrator Docker image"
      }
    }
  },
  "tags": ["type:app"]
}
