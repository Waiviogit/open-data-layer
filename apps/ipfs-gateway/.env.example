# =============================================================================
# ipfs-gateway — copy to .env next to this file.
# Shared IPFS / Docker ports: see repo root .env.example
# =============================================================================
PORT=3001

IPFS_API_URL=http://localhost:5001
# Optional: public gateway base for returned URLs (e.g. http://localhost:8080)
# IPFS_GATEWAY_URL=http://localhost:8080

# Comma-separated peer ipfs-gateway base URLs (e.g. http://host:3001/ipfs-gateway)
# IPFS_PEER_URLS=

# PIN_SYNC_INTERVAL_MS=300000
