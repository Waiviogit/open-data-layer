# Default compose: local builds + Postgres/Redis ports for host tooling (`pnpm nx serve`, migrations).
# Pre-built images: `docker-compose.staging.yml`, `docker-compose.production.yml`
# CI: `.github/workflows/build-images.yml`
include:
  - docker-compose.manual.yml
